/**
 * @deprecated This module is deprecated. Import from '@easy-ledger/core' instead.
 * Example: import { filterTransactions, computeTotals, isWithinRange } from '@easy-ledger/core'
 *
 * These exports are provided for backwards compatibility only and may be removed in a future version.
 */

export {
  getRateForCurrency,
  toBaseAmount,
  normalizeDateToYMD,
  getMonthRange,
  getLastMonthRange,
  isWithinRange,
  filterTransactions,
  computeTotals,
  computeCategoryTotals,
  buildCustomRange,
  getTodayRangeEnd,
} from '@easy-ledger/core'
